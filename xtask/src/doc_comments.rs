pub(crate) macro subdir {
    (zerOS) => {
        "The `zerOS` subdirectory (the kernel)"
    },
    (unwindtool) => {
        "The `unwindtool` subdirectory (a tool to inspect `DWARF` and custom `TROLL` formats)"
    },
    (macro-utils) => {
        "The `macro-utils` subdirectory (various macros mainly used in the kernel)"
    },
    (proc-macro-utils) => {
        "The `proc-macro-utils` subdirectory (various `proc-macro`s mainly used in the kernel)"
    },
    (generate-target) => {
        "The `generate-target` subdirectory (a JSON target specification generator)"
    },
    (docs) => {
        "The `docs` subdirectory (the zerOS documentation)"
    },
}
